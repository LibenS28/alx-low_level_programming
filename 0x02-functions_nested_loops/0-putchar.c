#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int _putchar(char c);

int main(void)
{
char b[7] = "_putchar";

for (int a = 0; a <= 7; a++)

{
_putchar (b[a]);
}
putchar('\n');
return (0);
}
